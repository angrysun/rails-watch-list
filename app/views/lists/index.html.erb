<div class="container">
  <h1>All the list</h1>

  <%= link_to "Add a new list", new_list_path %>

  <ul>
    <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
    <% end %>
  </ul>
</div>
