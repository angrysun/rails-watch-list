<div class="container">
  <h1>Add a new list</h1>
  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name %>
    <%= f.submit "Add List" %>
  <% end %>
  <%= link_to "Back", lists_path %>
</div>
