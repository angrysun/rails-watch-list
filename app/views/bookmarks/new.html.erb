<div class="container">
  <h1>Add a movie</h1>
  <div class="container">
    <%= simple_form_for [@list, @bookmark] do |f| %>
      <%= f.input :comment %>
      <%= f.association :movie %>
      <%= f.submit 'Add Movie' %>
    <% end %>
  </div>
  <%= link_to 'Back', list_path(@list) %>
</div>
